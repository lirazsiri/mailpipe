#!/bin/bash

if [[ $# < 1 ]]; then
    echo "Usage: (echo title; cat body) | $0 sender [ action_token ] [ error ]"
    exit 1
fi

if [ -n "$3" ]; then
    echo $3 1>&2
    exit 1
fi

echo sender=$1
echo action_token=$2

read TITLE
echo TITLE=$TITLE
echo === BODY STARTS
cat
echo === BODY ENDS
